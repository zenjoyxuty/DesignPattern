package com.xuty.designpattern.flyweight;

public class FlyweightIntroduce {
    //用来缓存可共享的对象，避免创建过多的对象
    //享元对象中部分状态可以共享
    //可以共享的为内部状态，不会随着环境变化而变化
    //不可以共享的状态为外部状态，会随着环境变化而变化
    //键是享元对象的内部状态，值是享元对象本身





}
